version: 2

sources:
  - name: jaffle_shop
    database: raw
    schema: jaffle_shop

    tables:
      - name: customers
      - name: orders
        config:
          freshness: # make this a little more strict
            warn_after: {count: 200, period: hour}
            error_after: {count: 150, period: hour}
          loaded_at_field: _ETL_LOADED_AT